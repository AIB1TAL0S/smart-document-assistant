[project]
name = "smart-document-assistant"
version = "0.1.0"
description = "Private, local document questions with Ollama embeddings and source citations"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn>=0.34,<1",
  "python-multipart>=0.0.20,<1",
  "pypdf>=6,<7",
  "python-docx>=1.2,<2",
]
